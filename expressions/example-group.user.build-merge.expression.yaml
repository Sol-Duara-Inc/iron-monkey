# yaml-language-server: $schema=https://cdrus.dev/schemas/0.1.0/expression.schema.json
group: example-group
author: user
expression: build-merge
description: Composite CI pattern — build a change, then merge it into the target branch.
produces:
  - expression: build
  - event: dev.cdevents.change.merged.0.3.0
