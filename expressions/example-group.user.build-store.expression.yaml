# yaml-language-server: $schema=https://cdrus.dev/schemas/0.1.0/expression.schema.json
group: example-group
author: user
expression: build-store
description: Composite CI pattern — build, then package and publish the resulting artifact.
produces:
  - expression: build
  - expression: artifact-store
