# RFC §7.3 — Workflow binding a composite Expression to concrete tools.
workflow:
  id: payments-mobile-release
  group: payment-engineering
  author: mchen
  name: Payments Mobile Release Pipeline
  cdrus:
    version: 0.1.0
  defaults:
    source: https://ci.example.com
  produces:
    - expression: payment-engineering/mchen/build-deploy
      overrides:
        dev.cdevents.build.started:
          tool: jenkins
          content:
            trigger: scheduled
        # service.deployed, not deployment.started: the vendored catalog has
        # no `deployment` subject (corrected 2026-08-16, with RFC §7.3).
        dev.cdevents.service.deployed:
          tool: spinnaker
          content:
            strategy: blue-green
