workflow:
  id: identity-svc-dev
  group: sar-app-identity-dev
  author: noah-berg
  name: Identity Service Build (dev)
  cdrus: {version: 0.1.0}
  defaults: {timeout_ms: 1200000}
  produces:
    - event: dev.cdevents.build.started
    - event: dev.cdevents.build.finished
      as: built
